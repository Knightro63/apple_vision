/// The facing of a camera.
enum CameraFacing {
  /// Front facing camera.
  front,

  /// Back facing camera.
  back,
}

/// The facing of a camera.
enum ImageOrientation{
  left,
  leftMirrored,
  right,
  rightMirrored,
  down,
  downMirrored,
  up,
  upMirrored
}

enum PictureFormat{jpg,jpeg,tiff,bmp,png,raw}